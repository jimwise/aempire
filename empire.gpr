Project Empire is

	for Source_Dirs use (".", "/usr/local/lib/ada/**");
	for Object_Dir use "./obj";

	for Main use ("main");

	package Builder is
		for Default_Switches ("Ada") use ("-g");
	end Builder;
     
	package Compiler is
		for Default_Switches ("Ada")
		    -- use ("-fstack-check", "-gnata", "-gnato", "-gnatE");
		    use ("-gnata", "-gnato", "-gnatE");
	end Compiler;

end Empire;
